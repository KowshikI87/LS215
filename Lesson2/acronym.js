function acronym(string) {
  //if contains ":" then return string.split(:)(0)

  //split by whitespace

  //split by "-" and return a string comprising of first two character of
  //split array
}

//use LS sollution and read the highlighted Note.