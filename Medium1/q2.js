////pretty much the solutioon is this:
//non rotate digit = String(num).slice(0, num.length - numDigitToRotate)
//digit to rotate = String(num).slice(num.length - numDigitToRotate))

//pop the digits to rate and push to new array
  //reversuing using a stack. join in a string.
//concat to non rotate digit and return the reuslt